<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/ProfileFragment">

    <fragment
        android:id="@+id/ProfileFragment"
        android:name="ivo.sociomile.android_sdk_sample.ProfileFragment"
        android:label="@string/first_fragment_label"
        tools:layout="@layout/fragment_second">

        <action
            android:id="@+id/action_ProfileFragment_to_HelpFragment"
            app:destination="@id/HelpFragment"
            app:enterAnim="@android:anim/fade_in"
            app:exitAnim="@android:anim/fade_out" />

    </fragment>
    <fragment
        android:id="@+id/HelpFragment"
        android:name="ivo.sociomile.android_sdk_sample.HelpFragment"
        android:label="@string/second_fragment_label"
        tools:layout="@layout/help_center_layout">
    </fragment>
</navigation>